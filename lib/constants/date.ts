/**
 * Date only format.
 *
 * @constant
 */
export const DATE_FORMAT = 'YYYY-MM-DD';

/**
 * Date only format (with slashes).
 *
 * @constant
 */
export const DATE_FORMAT_SLASHES = 'YYYY/MM/DD';

/**
 * Date only format (with slashes) for invoice items.
 *
 * @constant
 */
export const DATE_FORMAT_INVOICE_ITEM = 'MM/DD/YYYY';

/**
 * Date with time format.
 *
 * @constant
 */
export const DATE_TIME_FORMAT = 'YYYY-MM-DD HH:mm:ss';

/**
 * Timezones
 *
 * @constant
 */
export const TIMEZONES = {
  utc: 'UTC',
  toronto: 'America/Toronto',
  newYork: 'America/New_York',
};

/**
 * Date with time ISO format.
 *
 * @constant
 */
export const DATE_TIME_ISO_FORMAT = 'YYYY-MM-DDTHH:mm:ssZ';
