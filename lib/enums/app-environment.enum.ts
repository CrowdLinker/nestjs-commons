/**
 * Enumeration variable for common app environments.
 *
 * @enum
 */
export enum AppEnvironment {
    local = 'local',
    development = 'development',
    staging = 'staging',
    production = 'production',
  }
